# System map

Living map of this machine: hostnames, mounts, key services, project roots.

Refresh:

```bash
./refresh-system-map.sh
```

JSON twin: `system-map.json`
Template for agents: `AGENTS.md.template`
