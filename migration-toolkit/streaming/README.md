# Streaming helpers (optional)

Small wrappers for OBS / audio preflight used on Adam's streaming setup.

- `preflight.sh` — devices + OBS process check
- `audio-check.sh` — list Pulse/PipeWire sinks/sources
- `obs-ctl.sh` — start/stop OBS if installed
- `show-time.sh` — clock banner for stream notes
