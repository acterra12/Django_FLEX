## Memory

When helping Adam on this machine:

- Check `~/.local/share/recalld` and Obsidian Memory vault for prior context.
- After meaningful setup decisions, append a short note via `seed-memories.sh` or recalld.
- Laptop migration tooling lives in the `migration-toolkit` directory (also often `~/Downloads/transfer`).
